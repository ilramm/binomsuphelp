скрипт

1) будет считывать со строки логин и пароль к серверу
(ssh ip, user, port, password)

2) будет читать самые необходимы логи и выводить ошибки
(логи mysql, логи kernlog, логи nginx, логи пхп) binom.error.log
загвоздка 1: как скрипту понять какой файл является последним

3) бинарь, который можно запустить хоть где